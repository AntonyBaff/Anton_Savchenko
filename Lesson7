const car = {
    model: 'Toyota',
    year: 2007,
    showModel: function () {
        console.log(this.model);
    }
};

const anotherCar = {
    model: 'BMW',
    year: 2015
};

//car.showModel.call(anotherCar);

car.showModel.apply(anotherCar);

//console.log(car.model); 

//car.showModel();